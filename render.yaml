services:
  - type: web
    name: credit-ratings-analytics-temp
    env: node
    plan: free
    region: oregon
    branch: deploy/render-temp
    buildCommand: npm ci && npm run build -- --configuration development
    startCommand: npm run start:prod
    healthCheckPath: /dashboard
